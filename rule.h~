#ifndef _RULE
#define _RULE

#include <stdlib.h>
#include <stdio.h>

#include "hashtable_r.h"
#include "pattern.h"


typedef struct rule
{
	pattern* left;
	pattern* right;
}rule;

void printRule(rule* r);

rule* createRule(pattern* l, pattern* r);


#endif